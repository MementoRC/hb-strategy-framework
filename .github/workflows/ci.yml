# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [development, main]
  pull_request:
    branches: [development, main]

jobs:
  ci:
    uses: Claire-s-Monster/ci-framework/.github/workflows/reusable-ci.yml@36ff8bbc290576c8defc5e212faa3c14b6363899 # v2.9.2
    permissions:
      contents: write
      id-token: write
      pull-requests: write
      actions: read
      checks: write
      security-events: write
    with:
      pixi-environment: "ci"
      python-versions: '["3.10", "3.11", "3.12"]'
      os-matrix: '["ubuntu-latest"]'
      enable-performance: false
      enable-hygiene-check: true
      editable-install: true
      package-path: "."
      allowed-licenses: "MIT, Apache-2.0, BSD-3-Clause, BSD-2-Clause, ISC, PSF-2.0, LGPL-3.0"

  branch-policy:
    name: "Branch Policy"
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.base_ref == 'main'
    steps:
      - name: Check source branch
        run: |
          if [ "${{ github.head_ref }}" != "development" ]; then
            echo "❌ PRs to main must come from development branch"
            echo "   Source: ${{ github.head_ref }}"
            echo "   Expected: development"
            exit 1
          fi
          echo "✅ PR from development to main - allowed"

  security-scanning:
    name: "Security Scanning"
    runs-on: ubuntu-latest
    needs: [ci]
    if: always()
    steps:
      - name: Check security status
        run: |
          if [ "${{ needs.ci.result }}" != "success" ]; then
            echo "Security scanning failed"
            exit 1
          fi
          echo "Security scanning passed"

  ci-success:
    name: "CI Success"
    runs-on: ubuntu-latest
    needs: [ci]
    if: always()
    steps:
      - name: Check CI status
        run: |
          if [ "${{ needs.ci.result }}" != "success" ]; then
            echo "CI failed"
            exit 1
          fi
          echo "CI passed"
